name: LivingWorld GitHub Release

on:
  pull_request:
    branches:
      - '1.21.1'
    paths:
      - '.github/workflows/livingworld-release.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  startup-probe:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm workflow startup
        run: echo "LivingWorld release workflow startup is valid"
