# 0.3 Release Convergence — TDD Evidence

Date: 2026-08-12
PR: #160
Base: `645dfeb2134d31f398a288ec460d59f741ab97c0`

## Scope

This ledger records tests-first convergence of the post-`0.2.0` source capability set into a planned `0.3.0+1.21.1` release boundary.

The slice adds release-contract validation only. It does **not** change runtime behavior, persistence format/version, public config, provider protocol, installed-release status, Git tag or publication trigger.

Accepted boundary:

```text
immutable previous release 0.2.0+1.21.1
+ actual post-release feat: history
+ root CHANGELOG [Unreleased]
+ production world/recovery store contracts
+ current acceptance/deferred boundary
→ machine-readable 0.3 convergence contract
→ fail-closed CI/release-dry-run validation
→ planned candidate identity 0.3.0+1.21.1
```

`docs/releases/NEXT_RELEASE.txt` intentionally remains `0.2.0+1.21.1` throughout this PR. Changing it later is a separate release-request/shipping action.

## Initial contract RED

Tests-only head: `c9515419fa2c14a0928c48001af5fe20c719d36c`.

The initial Java policy test required:

- `docs/releases/0.3.0-convergence.json`;
- `scripts/ci/release_convergence.py`;
- `scripts/ci/test_release_convergence.py`;
- deterministic CI/release-dry-run execution of the contract;
- publication-trigger separation.

Observed CI #2827 / run `31574147184`:

```text
836 tests / 2 failures
```

Only the two intended convergence-contract/wiring assertions failed. The safety assertion that `NEXT_RELEASE.txt` still equals `0.2.0+1.21.1` passed.

## First GREEN implementation and regression exposure

Implemented:

- candidate contract with planned `0.3.0+1.21.1` identity;
- previous immutable release `0.2.0+1.21.1` / `e426f588efefa6aa48a6e536c4a998421bbda241`;
- exact post-release product capability inventory #123 through #158 by actual feature history;
- separate release-infrastructure inventory #121/#122;
- seven world-local stores and six auxiliary recovery stores;
- clean-state/no-migration boundary;
- exact manual/deferred acceptance boundary;
- standard-library fail-closed validator and focused tests;
- centralized execution through the existing `test_select_acceptance_suites.py` contract harness rather than adding workflow duplication.

First implementation head: `3926e1ae65affa81d72f70d913cb046e8de2e8cd`.

Observed CI #2837 / run `31574927071` failed in the new focused contract harness with:

```text
CHANGELOG [Unreleased] does not reference capability PR #123
```

Review showed this was a **validator bug**, not a changelog gap: the first implementation used `changelog.split("## [", 1)[0]`, which returned text before `[Unreleased]`.

Security on the same implementation independently found the two new tracked scripts were missing from `docs/security/APPROVED_SCRIPT_INVENTORY.json`.

## Parser regression RED

Tests-only parser hardening head: `a187cc2476280ebf98e468ae530b066450f6a2e8`.

A focused regression test imported the not-yet-implemented `extract_unreleased_section(...)` and required it to exclude both changelog-policy/header text and older released sections.

Observed CI #2839 / run `31575418686` failed exactly with:

```text
ImportError: cannot import name 'extract_unreleased_section'
```

No production/runtime code was involved.

## Parser GREEN + security inventory GREEN

The validator gained exact extraction of text between `## [Unreleased]` and the next release heading. The two new scripts were documented in the approved script inventory as non-network, transitively executed CI helpers rather than direct workflow commands.

Head: `97c80fbec55c84096415c43428d7f55c0ffd0b2c`.

Repository security policy #2478: **SUCCESS**.

The corrected focused validator then exposed the first real release-inventory gap:

```text
CHANGELOG [Unreleased] does not reference capability PR #151
```

Observed CI #2843 / run `31575589116` failed only at the convergence contract harness before expensive suites.

## Real changelog convergence gap

Inspection confirmed that the 0.3 social-graph foundation was already described in `[Unreleased]`, but unlike every other post-release product capability it did not explicitly carry its source PR number.

Fix head: `3aa6ab96b5a1ef269fc2e870b39d4d103b81e1b4`.

The existing entry was changed only from:

```text
Persistent bounded directed NPC↔NPC social-graph foundation for the 0.3 track
```

to:

```text
Persistent bounded directed NPC↔NPC social-graph foundation for the 0.3 track (PR #151)
```

No new product claim was added.

On that head:

- the canonical convergence contract harness passed in CI before expensive suite selection;
- repository security #2480 passed;
- complete CI/release/soak gates were allowed to continue.

Final exact-head workflow IDs are recorded in the PR body after the documentation/changelog freeze so this ledger does not require a post-verification head change.

## Machine-readable contract guarantees

`docs/releases/0.3.0-convergence.json` is validated against repository source of truth rather than treated as documentation-only prose.

The validator checks:

- schema version;
- candidate and previous release tag syntax;
- candidate version strictly newer than previous release;
- Minecraft suffix against `gradle.properties`;
- immutable previous release commit format/ancestry;
- exact requested tag equality when a later release request is present;
- exact publication trigger path;
- exact seven world-store inventory;
- exact six auxiliary recovery stores against both production and destructive recovery code;
- exact manual canary and installed-deferred boundaries;
- clean-state / no-migration policy;
- unique/sorted and disjoint capability/infrastructure PR inventories;
- every capability PR referenced from root `[Unreleased]`;
- in release workflow context, exact post-release `feat:` history matches the capability inventory.

## Preserved boundaries

This convergence slice does not:

- modify `docs/releases/NEXT_RELEASE.txt`;
- create or move a tag;
- publish a GitHub Release;
- claim installed 0.3 acceptance;
- change Minecraft/runtime behavior;
- change provider calls/schema;
- change world-store or persistence format versions;
- add migration/backfill code;
- change manual canaries or promote deferred cases to PASS;
- add another GitHub Actions workflow.

The canonical eight-workflow surface remains unchanged.

## Final delivery gate

After the release-plan/evidence/changelog freeze, PR #160 is accepted only on one exact head with:

- repository security policy SUCCESS;
- VillAIgence CI SUCCESS;
- Production Soak SUCCESS;
- GitHub Release dry-run SUCCESS with publication skipped;
- independent base→head review P0/P1/P2 = 0;
- unresolved review threads = 0;
- `docs/releases/NEXT_RELEASE.txt` still `0.2.0+1.21.1`.

Passing this gate completes **convergence planning only**. Exact `0.3.0+1.21.1` candidate creation remains the next separate shipping step.
